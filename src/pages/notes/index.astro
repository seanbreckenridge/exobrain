---
import NotesIndexTree from '../../components/NotesIndexTree.astro';
import { getCollection } from 'astro:content';

const posts = (await getCollection('notes')).filter((post) => !post.slug.startsWith("personal/"));
---

<NotesIndexTree posts={posts} />
