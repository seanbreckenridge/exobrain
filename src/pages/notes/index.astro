---
import NotesIndexTree from '../../layouts/NotesIndexTree.astro';
import { getCollection } from 'astro:content';

const posts = (await getCollection('notes')).filter((post) => !post.slug.startsWith("personal/"));
---

<NotesIndexTree posts={posts} title="Nodes Index" description="All my notes" />
